<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? "#{yield(:title)} — Instructor" : "Instructor Portal" %> | SkillForge</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <div class="dashboard-shell">
      <aside class="sidebar">
        <div class="sidebar-logo">
          <h2>⚡ SkillForge</h2>
          <div class="role-tag">Instructor Portal</div>
        </div>

        <nav class="sidebar-nav">
          <div class="sidebar-section-label">Overview</div>
          <%= link_to instructor_dashboard_index_path, class: "#{active_nav(instructor_dashboard_index_path)}" do %>
            <span class="nav-icon">🏠</span> Dashboard
          <% end %>

          <div class="sidebar-section-label">Teaching</div>
          <%= link_to "#", class: "#{active_nav("#")}" do %>
            <span class="nav-icon">📚</span> My Courses
          <% end %>
          <a href="#" style="opacity:0.4;cursor:not-allowed"><span class="nav-icon">📝</span> Assessments <span style="font-size:0.65rem;margin-left:auto;color:var(--text-muted)">Phase 5</span></a>
          <%= link_to "#", class: "#{active_nav("#")}" do %>
            <span class="nav-icon">👥</span> Enrollments
          <% end %>

          <div class="sidebar-section-label">Catalog</div>
          <%= link_to "#" do %>
            <span class="nav-icon">🌐</span> Public Catalog
          <% end %>
        </nav>

        <div class="sidebar-footer">
          <%= link_to edit_user_registration_path, class: "sidebar-user", style: "text-decoration: none; color: inherit; display: flex; transition: transform 0.2s; cursor: pointer;" do %>
            <div class="sidebar-avatar" style="background: var(--gradient-instr); padding: 0;">
              <% if current_user.respond_to?(:avatar) && current_user.avatar.attached? %>
                <%= image_tag current_user.avatar, style: "width: 100%; height: 100%; object-fit: cover; border-radius: inherit;" %>
              <% else %>
                <%= current_user.name.first.upcase rescue "U" %>
              <% end %>
            </div>
            <div class="sidebar-user-info">
              <div class="name" style="text-decoration: underline; text-decoration-color: transparent; transition: text-decoration-color 0.2s;"><%= current_user.display_name %></div>
              <div class="email"><span class="badge badge-instructor">Instructor</span></div>
            </div>
          <% end %>
          <div style="margin-top: var(--space-md);">
            <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-ghost btn-sm", style: "width:100%;justify-content:center;" %>
          </div>
        </div>
      </aside>

      <main class="main-content">
        <%= render "shared/flash" %>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
